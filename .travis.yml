# Sample .travis.yml for R projects.

language: r
warnings_are_errors: true
sudo: required
before_script:
    - cat ${TRAVIS_BUILD_ID}
notifications:
  email:
    on_success: change
    on_failure: change
